version: '3'

services:
  portfolio_svelte:
    restart: unless-stopped
    build:
      context: /home/sachinda
      dockerfile: ./repos/portfolio-svelte/dockerfile
    volumes:
      - /home/sachinda/disks/portfolio-svelte:/app
    ports:
      - '3100:3100'
    command: ['node', 'build']
